# Copy to .env and fill in. Never commit .env.

# Published port of the reverse proxy
HTTP_PORT=8080

# API
BACKEND_PORT=3002

# PostgreSQL (POSTGRES_HOST is the compose service name)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=eatai
POSTGRES_PASSWORD=change-me
POSTGRES_DB=eatai

# OpenAI
OPENAI_API_KEY=
OPENAI_ASSISTANT_ID=
